# Pipeline dependencies, pinned to the versions this project was last
# verified against (all 9 stages green, 21/21 Great Expectations checks).
#
# NOTE: apache-airflow is NOT listed here. It depends on POSIX-only modules
# (pwd/fcntl) and cannot be installed on native Windows. The DAG is run via
# Docker instead -- see docs/reproduction_guide.md Section 5.

# Core
pandas==3.0.5
numpy==2.5.2
polars==1.43.2
pyarrow==25.0.1

# Big-data processing (requires Java 17+; see reproduction guide Section 0)
pyspark==4.2.0

# Data quality
great_expectations==1.20.0

# Versioning
dvc==3.67.1

# Dev / notebooks
jupyter==1.1.1

# Dataset acquisition (Source #1, real Kaggle data)
kaggle==2.2.4

# Simulated API + misc
requests==2.34.2
flask==3.1.3

# Visualization
matplotlib==3.11.1   # notebook plots
plotly==6.9.0        # dashboard
